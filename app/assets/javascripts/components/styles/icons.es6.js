import { StyleSheet } from 'aphrodite';

const iconStyles = StyleSheet.create({
  links: {
    height: 'auto',
    marginTop: 15,
    marginLeft: 0,
  },

  linkIcon: {
    color: '#555',
    padding: 0,
    marginRight: 10,
    width: 24,
    height: 24,
  },

  inline: {
    float: 'left',
  },
});

export default iconStyles;
